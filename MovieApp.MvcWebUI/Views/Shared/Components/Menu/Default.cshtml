@model IEnumerable<MovieApp.MvcWebUI.Models.Category>

<ul class="list-group">
    <li class="list-group-item active">MENU</li>
    @foreach (var item in Model)
    {
        <li class="list-group-item">@item.Name</li>
    }
</ul>